# カスタマイズガイド

貴院の運用に合わせて内容を変更する方法です。

| 変更したいもの | 方法 | ビルド必要 |
| --- | --- | --- |
| 医療機関名・連絡先・運動教室の案内 | ⚙️ 設定画面 | 不要 |
| 文字サイズ・ふりがな・医師メモの表示 | ⚙️ 設定画面 | 不要 |
| 診療報酬の点数 | `src/data/fees.ts` | 必要 |
| 薬剤の商品名・費用・注意点 | `src/data/drugs.ts` | 必要 |
| 運動メニュー・処方量 | `src/data/exercises.ts` | 必要 |
| 栄養の目標量・生活指導 | `src/data/nutrition.ts` | 必要 |
| 説明スライドの文言・順序 | `src/slides/*.tsx` | 必要 |
| 図表 | `src/components/figures/*.tsx` | 必要 |
| パンフレットの構成 | `src/components/pamphlet/Pamphlet.tsx` | 必要 |

ビルドが必要な変更のあとは：

```bash
npm test           # 臨床ロジックが壊れていないか確認
npm run build      # dist/ を再生成
# dist/ を配置し直す
```

---

## 1. 診療報酬の点数を更新する

`src/data/fees.ts`

```ts
// ① マスタ全体の最終確認日を更新する
export const FEE_MASTER_VERIFIED_AT = '2026-08-15'   // ← 確認した日付に

export const FEE_ITEMS: FeeItem[] = [
  {
    id: 'undouki-reha-1',
    code: 'H002',
    name: '運動器リハビリテーション料（Ⅰ）',
    points: 185,          // ← ② 最新の点数表の値に
    unit: '1単位（20分）',
    requirement: '...',   // ← ③ 算定要件が変わっていれば更新
    facilityStandard: '必要（施設基準の届出）',
    disease: ['osteoporosis', 'ra', 'kneeOA'],
    meta,
    sources: ['mhlw-fee-r8'],
  },
]
```

- `points: null` にすると、画面に「**要確認**」と表示されます。
  **確認できていない項目は、推測で数字を入れずに `null` にしてください。**
- 項目を追加した場合は、`src/logic/karte.ts` の `suggestFees()` で
  どの条件のときに候補として出すかを設定します。

### 算定候補の出し方を変える

`src/logic/karte.ts`

```ts
export function suggestFees(session: Session): FeeItem[] {
  const out: FeeItem[] = []

  // 例：運動器リハビリを導入したときだけリハ料を候補に出す
  if (session.plan.exercisePathway.includes('undoukiRehab')) {
    out.push(...FEE_ITEMS.filter((f) => f.id.startsWith('undouki-reha-')))
  }

  // 例：自院で新設した加算を、骨粗鬆症で薬を開始したときに出す
  if (session.disease === 'osteoporosis' && session.plan.drugIds.length > 0) {
    out.push(...FEE_ITEMS.filter((f) => f.id === 'my-clinic-kasan'))
  }
  // ...
}
```

---

## 2. 薬剤を追加・変更する

`src/data/drugs.ts`

```ts
{
  id: 'my-drug',                          // 一意なID
  generic: '一般名',
  brands: ['院内採用品の商品名'],           // ← 貴院の採用品に置き換える
  cls: 'bisphosphonate',                  // 薬効分類（型 DrugClass 参照）
  disease: ['osteoporosis'],              // どの疾患の画面に出すか
  route: '内服（週1回）',
  plain: '患者さん向けのやさしい説明。1〜2文で。',
  howItWorks: 'どう効くのか。たとえを使って。',
  schedule: 'のみ方・打ち方。具体的な曜日や時間帯まで。',
  cautions: [                             // パンフレットに印刷される
    '生活上の注意を最優先で。',
    '緊急連絡が必要な症状。',
  ],
  clinicalNote: '医師向け：用量・禁忌・モニタリング。添付文書の要点。',
  durationLimit: '通算24か月まで',          // 任意
  costHint: '3割負担で月およそ◯円',          // 任意
  sources: ['op-gl-2025'],                // src/data/sources.ts のID
}
```

### 分類による自動処理

`cls` に応じて、以下が自動で働きます（`src/data/drugs.ts` の末尾）。

| 分類 | 自動で起きること |
| --- | --- |
| `bisphosphonate` / `denosumab` / `romosozumab` | 歯科連携のスライドとパンフレットのページが追加され、カルテ文に顎骨壊死の説明が入る |
| `teriparatide` / `abaloparatide` / `romosozumab` | 逐次療法のスライドが追加され、「後続薬の予約確保」が今日のToDoに入る |
| `csdmard`（MTX） | 週1回カレンダーのスライド、血液検査のToDoが入る |

新しい分類を追加する場合は `src/types/index.ts` の `DrugClass` に追記します。

---

## 3. 運動メニューを追加する

### 既存の図を使う場合

`src/data/exercises.ts` に項目を追加し、`figure` に既存のキーを指定します。

```ts
{
  id: 'my-exercise',
  name: '正式な名前（カルテ記載に使われます）',
  shortName: '記録表用の短い名前',
  category: 'strength',            // balance / strength / weightBearing / backExtensor
                                   // aerobic / rom / jointProtection / aquatic
  disease: ['osteoporosis'],
  level: 'standing-support',
  steps: ['手順を1文ずつ。', '患者さんが読んで実行できる粒度で。'],
  dose: { reps: '10回', sets: '2〜3セット', frequency: '1日2回' },
  stopRules: ['中止基準。安全に関わることを最初に。'],
  figure: 'squat',                 // ExerciseFigureKey のいずれか
  clinicalNote: 'PT向けの補足（任意）',
  sources: ['op-gl-2025'],
}
```

### 新しい図を追加する場合

1. `src/types/index.ts` の `ExerciseFigureKey` にキーを追加
2. `src/components/figures/exercise.tsx` に描画関数を追加し、`MAP` に登録

```tsx
function MyExercise() {
  return (
    <Frame title="運動の名前" desc="読み上げ用の説明" caption="10回 × 2セット">
      <Table x={22} y={112} w={52} />
      <StickPerson
        x={ORIGIN.x}
        y={ORIGIN.y}
        pose={pose({ kneeR: [26, 100], footR: [16, 122] })}  // STAND から差分で指定
        color={PALETTE.ink}
      />
      <Floor x1={16} x2={224} y={FLOOR_Y} />
    </Frame>
  )
}

const MAP: Record<ExerciseFigureKey, () => ReactElement> = {
  // ...
  myExercise: MyExercise,
}
```

`StickPerson` の `pose` は、直立姿勢（`STAND`）からの差分だけを書けば済みます。
座標は「頭の中心を原点、y は下向き」です。

### おすすめの組み合わせを変える

`src/data/exercises.ts` の `RECOMMENDED_SETS` を編集します。
医師が1クリックで一括選択できるボタンになります。

```ts
export const RECOMMENDED_SETS = {
  osteoporosis: [
    {
      label: '当院の標準セット',
      ids: ['one-leg-stand', 'squat', 'back-extension'],
      note: 'ボタンにカーソルを合わせたときに表示される説明',
    },
  ],
  // ...
}
```

---

## 4. 説明スライドを変える

`src/slides/osteoSlides.tsx` / `raSlides.tsx` / `kneeSlides.tsx`

```tsx
{
  id: 'my-slide',                    // 一意なID（実施記録に残ります）
  group: '治療',                      // 現状 / 病気のしくみ / 治療 / 運動療法 / 生活 / 見通し
  title: '患者さん向けの見出し',        // 画面に大きく出ます
  lead: '見出しの下の一文（任意）',
  figure: <MyFigure value={x} />,     // 任意
  points: ['要点を箇条書きで。3〜5個まで。'],
  highlight: { tone: 'warn', text: 'とくに強調したいこと' },
  talk: ['医師向けトークスクリプト。実際の言い方の例。'],
  cite: ['op-gl-2025'],
  when: (s) => s.osteo.fractures.length > 0,   // 表示条件（任意）
}
```

### 順序を変える

配列の順序がそのままスライドの順序です。`slides` 配列内で位置を移動します。

### 患者さんのデータを反映する

`buildOsteoSlides(session)` の引数から `session` を参照できます。

```tsx
const a = assessOsteoporosis(session.osteo, session.patient)
// a.adoptedYam, a.riskTier などを title や figure に渡す
```

---

## 5. パンフレットの構成を変える

`src/components/pamphlet/Pamphlet.tsx`

### ページを追加する

1. `PamphletSection` に識別子を追加
2. `SECTION_LABELS` にラベルと説明を追加（選択ボタンになります）
3. `Pamphlet` の中に `{has('新しいID') && <MySheet session={session} />}` を追加
4. `MySheet` を実装

```tsx
function MySheet({ session }: { session: Session }) {
  return (
    <Sheet title="ページの見出し" page="セクション名">
      <PFig w={160} className="mb-3">
        <MyFigure />
      </PFig>
      <Box title="■ 見出し" tone="warn">
        <ul className="space-y-1">
          <li>・内容</li>
        </ul>
      </Box>
      <WriteLines n={3} label="■ 書き込み欄の見出し" />
    </Sheet>
  )
}
```

### 印刷レイアウトのコツ

| やりたいこと | 方法 |
| --- | --- |
| A4 1ページを1つの `Sheet` にする | `<Sheet>` が `break-after: page` を持っています |
| 途中で分割されたくないブロック | `avoid-break` クラス（`break-inside: avoid`） |
| 図が大きすぎてページに収まらない | `<PFig w={150}>` で幅の上限（mm）を指定。**図は幅を絞ると縦も短くなります** |
| 高さのばらつくカードを詰めて配置 | 親を `columns-2 gap-3`、子に `mb-3 break-inside-avoid`（grid より紙面が埋まります） |
| 印刷時だけ薄い背景を付ける | `print-fill-light` クラス |
| 画面だけ／印刷だけに出す | `no-print` / `print-only` クラス |

印刷用CSSは `src/index.css` の `@media print` にまとまっています。

### ページ数を確認する

ブラウザの印刷プレビューで確認できます。開発時は Playwright でも確認できます。

```bash
npm run build && npm run preview
# 別のシェルで
node -e "
const { chromium } = require('playwright');
(async () => {
  const b = await chromium.launch();
  const p = await b.newPage();
  await p.goto('http://127.0.0.1:4173/');
  /* 画面を操作してパンフレット画面まで進める */
  await p.emulateMedia({ media: 'print' });
  await p.pdf({ path: 'check.pdf', format: 'A4', printBackground: true });
  await b.close();
})();
"
```

---

## 6. 疾患を追加する

腰痛・肩関節疾患・骨折後のリハビリなどを追加する場合の手順です。

1. `src/types/index.ts` の `DiseaseKey` に追加
2. `src/state/session.ts` の `DISEASE_LABEL` / `DISEASE_SUBLABEL` / `createSession` に追加
3. 入力フォームを `src/screens/forms/` に作成し、`InputScreen.tsx` に接続
4. 判定ロジックを `src/logic/` に作成し、**テストを同時に書く**
5. `src/screens/forms/AssessmentPanel.tsx` に判定表示を追加
6. `src/slides/` にスライドビルダーを作成し、`src/slides/index.ts` に登録
7. `src/data/drugs.ts` / `exercises.ts` の `disease` にキーを追加
8. `src/components/pamphlet/Pamphlet.tsx` の `MechanismSheet` に分岐を追加
9. `src/screens/HomeScreen.tsx` の `CARDS` にカードを追加
10. `src/logic/karte.ts` にカルテ文の生成を追加

---

## 7. 変更を安全に行うための順番

臨床判断のロジックを変えるときは、**テストを先に書き換えてください。**

```bash
# 1) テストに新しい基準を書く（この時点で失敗する）
vi src/logic/osteoporosis.test.ts
npx vitest run   # 失敗を確認

# 2) 実装を直す
vi src/logic/osteoporosis.ts
npx vitest run   # 全件成功を確認

# 3) 型と本番ビルド
npx tsc --noEmit -p tsconfig.app.json
npm run build

# 4) パンフレットを実際に1部印刷して目視確認
```

この順番にすると、**「基準を変えたつもりが別の条件を壊していた」**という事故を防げます。
実際にこのシステムの開発中も、立ち上がりテストの「立てない」と「未実施」の混同、
ACR/EULAR 2010分類基準の階層の取り違えを、テストが検出しました。
